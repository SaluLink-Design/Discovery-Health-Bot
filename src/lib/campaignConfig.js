/** When true, member campaign mode: plain-language labels, no clinician nav, demo-first setup. */
export const CAMPAIGN_MEMBER_MODE = true;

/** Literacy quiz soft-gate before treatment, medication, and hospitals features. */
export const CAMPAIGN_LITERACY_ENABLED = true;

export const SCHEME_SOURCE_NOTE =
  'Based on Discovery Health 2026 member guides. Always confirm with Discovery Health directly.';
